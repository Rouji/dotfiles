alias dot='git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'

INIT
git init --bare $HOME/.dotfiles
dot config --local status.showUntrackedFiles no

CLONE
git clone --bare https://github.com/Rouji/dotfiles.git ~/.dotfiles
dot config --local status.showUntrackedFiles no
dot reset --hard HEAD
dot submodule init
dot submodule update

(N)VIM
vim-plug will be downloaded on first launch, run :PlugInstall to install the plugins

TMUX
`ctrl+a I` in tmux to install plugins
